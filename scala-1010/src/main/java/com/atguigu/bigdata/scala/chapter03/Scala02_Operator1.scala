package com.atguigu.bigdata.scala.chapter03

object Scala02_Operator1 {
    def main(args: Array[String]): Unit = {
        
        // TODO 运算符
        // Scala是一个完全面向对象的的语言。所以运算符也和对象相关。
        // 所有的运算符都是方法
        // Scala中万物皆对象，所以数字也是一个对象
        //val sum = 1.+(1)
        //val result = 1./(2)
        // 对象的方法，如果只有一个参数的情况下，点可以省略，并且小括号可以省略
        //val sum1 = 1 + 1
        
        //println(sum)
        println(1.toString())
        println(1 toString())
        println(1 toString)
        // 阶乘
        //println(5 !)
    }
}
